<!-- <table>
  <tr>
    <th> Name </th>
    <th> Height </th>
    <th> Weight </th>
    <th> Color </th>
    <th> GPA </th>
  </tr>
  <% @students.each do |s| %>
  <tr>
    <td><%=s.name%></td>
    <td><%=s.height%></td>
    <td><%=s.weight%></td>
    <td><%=s.color%></td>
    <td><%=s.gpa%></td>
  </tr>
  <% end %>
</table> -->
<%= link_to "New Student", new_student_path %>
<% @students.each do |s| %>
<div style="margin-top:5px; height:<%=s.height%>px; width:<%=s.height%>px;
  background-color:<%=s.color%>; display:inline-block">
  <%=s.name%>
</div>
<% end %>
